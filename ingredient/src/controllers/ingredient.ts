import { Request, Response } from 'express';

export const getIngredients = (req: Request, res: Response) => {
    return res.status(200).json('stub');
};

export const postIngredient = (req: Request, res: Response) => {
    return res.status(200).json('stub');
};

export const patchIngredient = (req: Request, res: Response) => {
    return res.status(200).json('stub');
};

export const deleteIngredient = (req: Request, res: Response) => {
    return res.status(200).json('stub');
};
